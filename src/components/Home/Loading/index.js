import './style.scss';

// loader page
const Loading = () => (
  <section className="loading-section">
    <p className="loading">Nos peintures sèchent ...</p>
  </section>
);

export default Loading;
