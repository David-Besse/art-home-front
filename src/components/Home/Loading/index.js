import './style.scss';

/**
 * Loader
 * @returns (HTML.Element)
 */

const Loading = () => (
  <section>
    <p className="loading">Nos peintures sèchent ...</p>
  </section>
);

export default Loading;
